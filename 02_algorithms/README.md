# Algorithms

This area contains algorithmic exercises that build on the Python foundations.

- `number_theory/` — prime-number and factorial-style exercises.
The implementations use Python, but their primary learning goal is algorithmic
reasoning rather than Python syntax.
